import styled from 'styled-components';
import { Accordion } from '../components/Accordion';

const Result = () => {
  return (
    <div>
      <Accordion />
      <button>버튼자리</button>
    </div>
  );
};

export default Result;
